import { ColoresDirective } from './colores.directive';

describe('ColoresDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoresDirective();
    expect(directive).toBeTruthy();
  });
});
